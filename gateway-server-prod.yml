# Keycloak은 글로벌 (gateway는 secret 미사용)

# Logging
logging:
  level:
    com.muscat.gateway: INFO
    org.springframework.cloud.gateway: INFO
    org.springframework.security: WARN
    reactor.netty: INFO
